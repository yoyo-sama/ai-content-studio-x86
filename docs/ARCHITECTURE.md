# Architecture

## Vue d'ensemble

```
Navigateur ── index.html (nginx :8090, statique)
   │  fetch/WS
   ├── ComfyUI :8188      REST /prompt /history /queue /view /upload/image /object_info /models/*
   │                      WS /ws?clientId=…  (progress, executed, execution_success/error)
   ├── Ollama :11434      /api/chat (gemma4:e4b : enrichissement + découpage en plans)
   └── APIs cloud (opt.)  OpenAI gpt-image-1, Gemini gemini-2.5-flash-image (clé en sessionStorage)
```

Tout est côté client. Les graphes ComfyUI sont soit des **templates API substitués**, soit **construits dynamiquement en JS**, puis POSTés à `/prompt`. Le suivi se fait par WebSocket (jobs, assets) avec relecture d'`/history` en fin de job (`collectHistory`) pour les sorties que le WS ne remonte pas et pour l'extraction des prompts.

## Formats de workflows

**Format UI** (`workflows/*.json`) : graphes complets exportés ComfyUI (`nodes`/`links`/`widgets_values`, éventuellement `definitions.subgraphs`). Usage : drag-drop dans l'interface ComfyUI. Non consommables par `/prompt` directement → `tools/convert.py` les convertit en API.

**Format API** (`workflows/api/*.json` et builders JS) : `{ "<id>": { "class_type", "inputs": { nom: valeur | [refId, slot] } } }`. Les templates de `api/` contiennent des placeholders substitués textuellement par `buildGraph()` :

| Placeholder | Remplacé par |
|---|---|
| `"{{PROMPT}}"`, `"{{NEGATIVE_PROMPT}}"`, `"{{IMAGE}}"` | chaînes JSON |
| `"{{SEED}}"`, `"{{WIDTH}}"`, `"{{HEIGHT}}"`, `"{{BATCH}}"` | nombres |
| `"{{DURATION}}"` (s), `"{{FRAMES}}"` (= durée×fps+1, fps=25 par défaut) | nombres |

**manifest.json** (v2, deux sections) :
- `pipelines:[{id, label, scenario?, controls, defaults?, fps?}]` — pilote l'UI. `scenario` (chaîne ou liste) restreint le pipeline à un/des scénario(s) ; absent = disponible partout. `controls` = liste des champs de formulaire à afficher, parmi `duration, shots, hold, variants, ratio, audio, image, markets, seq, mode` (mappés aux wraps par `CONTROL_WRAPS` — `mode` → `modeWrap`, le sélecteur Auto/Réalisateur). `defaults.duration` = valeur remise au changement de pipeline. `fps` propagé à `buildGraph` pour `{{FRAMES}}` (ex. Wan = 16). Un fallback JS (`PIPELINES_FALLBACK`) couvre les 7 pipelines de base si la section manque (y compris `storyboard_v2`, à l'identique du manifest).
- `workflows:[{id, label, pipeline, model, file, enrich?, builder?, engine?, fps?, anchor?}]`. `file:null` + `builder` = graphe construit en JS (dispatch via le registre `BUILDERS`). `enrich:"builtin"` (Ernie, templates LTX) court-circuite l'auto-enrichissement gemma. `engine:"ernie"` bascule les builders storyboard/campagne sur Ernie-Image. `anchor` (`storyboard_v2` uniquement) = chemin du template API **dual** (2 images d'entrée) utilisé pour chaque keyframe (`api/qwen_edit_dual.json`, Qwen-Image-Edit 2509 + LoRA Lightning) — changer de modèle d'ancrage = changer cette seule ligne du manifest. L'ancien template mono-image (`api/qwen_edit_i2i.json`, 1 seule image d'entrée) reste sur disque, non référencé : bascule arrière = remettre son chemin sur cette ligne.

## Flux Generate (handler du bouton)

1. Auto-enrichissement gemma optionnel (sauf `enrich:"builtin"`).
2. Calcul seed/ratio/batch/durée.
3. Si `wf.builder` : dispatch via le registre `BUILDERS` (`flf2v`→`generateSequence`, `storyboard_v2`→ selon `directorMode` : `generateStoryboardV2` (mode Auto, chaîne complète) ou `directorStep1` (mode Réalisateur, ne lance que l'étape 1 — la suite est pilotée par les boutons de `directorZone`), `campaign_full`→`generateCampaignFull`). Un nouveau builder = une fonction `(seed, width, height)` + une entrée dans `BUILDERS`.
4. Sinon : boucle `jobsSpec` (1 entrée, ou 1/marché sélectionné en image2image) → `tryCloudGeneration` (modes hybride/cloud, images seulement) sinon template local + `applyLtxAudio` éventuel → POST → `trackJob`.

## Builders de graphes JS (le cœur du projet)

- `makeGraphBuilder()` → `{g, add}` ; ids séquentiels "1"…
- `addFluxShared/addFluxShot` : branche image Flux2 Klein 9B (4 steps, cfg 1, négatif = `ConditioningZeroOut` du positif, sigmas via `Flux2Scheduler`).
- `addErnieShared/addErnieShot` : branche image Ernie-Image base (`KSampler` 20 steps, cfg 4, euler/simple, encodeur ministral-3-3b type flux2, VAE flux2) — images plus cohérentes/fidèles au brief que Flux2 (mais 20 steps vs 4). Utilisée sans condition par les DEUX planches d'ancrage de `storyboard_v2` (`buildCharsheetGraph` pour la fiche personnage, `buildLocsheetGraph` pour la fiche décor — même branche, prompt et négatif différents) ; pour `campaign_full`, c'est une variante sélectionnée via `engine: "ernie"` dans le manifest (sinon Flux2 par défaut).
- `addLtxShared` : checkpoint LTX 2.3 dev-fp8 + text encoder gemma_3_12B + LoRA distillée (0.5) + `SamplerEulerAncestral(eta 0)` + `ManualSigmas` 8 steps (`FLF2V_SIGMAS`) + audio VAE optionnel + LoRA d'enhancer sur le clip (`enh`). Utilisée par `buildFLF2VGraph` (pipeline `sequence2video`) — `storyboard_v2` ne l'utilise pas : ses cuts vidéo passent par le template `api/ltx_i2v.json` (2 passes, prompt-enhancer intégré), pas par ce builder JS.
- `addLtxEnhance(add, sh, prompt, seed)` : prompt-enhancer intégré LTX (`TextGenerateLTX2Prompt` sur le clip LoRA-isé) — indispensable car le distillé tourne à cfg 1 (voir LESSONS piège n°5) ; appliqué à chaque segment FLF2V et présent dans les templates api t2v/i2v (`enrich:"builtin"`).
- `addFLF2VChain` : segments first/last-frame (2×`LTXVPreprocess` → 2×`LTXVAddGuide` frame_idx 0/-1 strength 0.7 → sampler → `LTXVCropGuides` → `VAEDecodeTiled`), audio joint optionnel par segment, concat images `ImageBatch` + audio `AudioConcat`. Utilisée uniquement par `buildFLF2VGraph` (`sequence2video`) — sans notion de tenue de plan final (voir plus bas pour l'équivalent `storyboard_v2`).
- `addGrid` : contact-sheet `ImageStitch`, grille carrée adaptative (`cols = ceil(sqrt(n))`, ex. 4→2×2, 8/9→3×3, 16→4×4), lignes remplies à gauche et équilibrées (une ligne trop courte serait étirée par `match_image_size`).
- `applyLtxAudio(graph)` : ajoute l'audio à un graphe API LTX existant — si `LTXVSeparateAVLatent` présent (t2v 2-passes) il ne manque que `LTXVAudioVAEDecode`+branchement ; sinon (i2v) enveloppe le sampler (`LTXVEmptyLatentAudio`+`LTXVConcatAVLatent` avant, `LTXVSeparateAVLatent` après, re-câblage des consommateurs). Utilisée par `generateCampaignFull` pour le teaser ; `storyboard_v2` n'en a pas besoin, `buildAnimaticGraph` concatène directement les pistes audio déjà décodées de chaque cut (`GetVideoComponents` slot 1 + `AudioConcat`).
- `mergeGraph(g, other, prefix)` : fusion de graphes API par préfixage des ids (utilisé pour insérer le teaser t2v dans la campagne complète).

### `storyboard_v2` — orchestration multi-jobs côté page (pas un graphe unique)

Contrairement aux autres builders (un seul graphe API POSTé une fois), `storyboard_v2` est une **séquence de jobs ComfyUI indépendants orchestrée en JS** (`generateStoryboardV2` pour le mode Auto, `directorStep1/2/3` pour le mode Réalisateur) : chaque étape attend la précédente via `waitForJobs` (poll `/history`, intervalle 1.5 s) puis récupère l'image/vidéo produite et la ré-uploade comme entrée de l'étape suivante via `reupload` (pattern `/view` → `/upload/image`). Depuis l'évolution **ancrage double** (2026-07-17), le pipeline fige DEUX fiches structurées gemma (personnage ET décor) et produit DEUX planches Ernie, toutes deux servant d'entrée à chaque keyframe via un template Qwen-Edit **dual** (2 images). Étapes, dans l'ordre réel du code :

1. **Fiche personnage** (`characterSheetFromBrief`) : un appel gemma4 **structuré** (`format:"json"`) qui fige une description dense du personnage principal du brief, en anglais, sous forme d'objet `{"face","hair","outfit","accessories","palette"}` — chaque champ ~10-20 mots, sans mention de caméra/scène/action. `fieldText` aplatit les valeurs (gemma renvoie parfois des objets imbriqués) puis les 5 champs sont concaténés en une seule chaîne `charDesc`, réutilisée telle quelle par toutes les étapes suivantes.
2. **Fiche décor** (`locationSheetFromBrief`) : appel gemma4 symétrique et indépendant, qui fige le lieu principal du brief en objet `{"place","architecture","materials","lighting","palette"}` (sans mention de caméra/personnage/action), concaténé en une seule chaîne `locDesc`.
3. **Charsheet image** (`submitCharsheetJob` → `buildCharsheetGraph`) : Ernie-Image base (`addErnieShared/addErnieShot`, 20 steps cfg 4), 1920×1088, sur fond gris neutre studio — turnaround plein pied 4 poses (face/¾/profil/dos), portrait visage en gros plan, rangée d'expressions (neutre/souriant/surpris/déterminé), gros plans costume, bandeau de nuances couleur. Négatif `CHARSHEET_NEG` (texte/cadres/planches/dessin banni, cfg 4 → le négatif agit réellement). Sortie `studio/story/charsheet`. C'est l'**ancre d'apparence** (image 1) de chaque keyframe.
4. **Locsheet image** (`submitLocsheetJob` → `buildLocsheetGraph`) : même branche Ernie, fond neutre sombre, plusieurs plans larges du lieu sous des angles différents + rangée de plans moyens de structures clés + gros plans matières/détails. Négatif `LOCSHEET_NEG` = `CHARSHEET_NEG` + bannissement explicite des personnes (une planche décor ne doit montrer personne). Sortie `studio/story/locsheet`. C'est l'**ancre d'environnement** (image 2) de chaque keyframe. Les deux planches (3 et 4) sont soumises en parallèle, avant même la fin du découpage en plans.
5. **Liste de plans** (`shotListFromBrief(n)`) : un troisième appel gemma4 (indépendant des deux précédents) qui découpe le brief en exactement N plans `{scene, angle, motion}` — un seul personnage par plan, scène/décor imposés par le brief, uniquement caméra/moment qui varient. Repli statique (`SHOT_ANGLES`/`FALLBACK_MOTIONS`) si gemma échoue ou renvoie un format inattendu (`normShot` normalise aussi les objets imbriqués que gemma renvoie parfois à la place de chaînes).
6. **N keyframes ancrées DUAL** (`submitKeyframeJob`, 1 job Qwen-Edit par case) : le template désigné par le champ manifest **`anchor`** (`api/qwen_edit_dual.json`, Qwen-Image-Edit 2509 + LoRA Lightning 4 steps, cfg 1) reçoit **2 images d'entrée** — `image` = charsheet, `image2` = locsheet (placeholders `{{IMAGE}}`/`{{IMAGE2}}` substitués par `buildGraph`) — et le prompt `keyframePrompt(shot, charDesc)`, qui précise explicitement d'utiliser la 1ʳᵉ image comme guide d'apparence du personnage et la 2ᵉ comme guide d'environnement, sans split-screen/diptyque/collage. Dans le graphe (`workflows/api/qwen_edit_dual.json`), seule l'image 1 (charsheet) alimente le `VAEEncode` qui devient le latent de départ du `KSampler` ; l'image 2 (locsheet) n'alimente que les nœuds `TextEncodeQwenImageEditPlus` (conditioning positif ET négatif) — **elle ne touche jamais le latent**. Ce prompt reste entièrement **positif** (cfg 1 → negative prompt ignoré, voir LESSONS) : une seule instance du personnage, rendu **"photorealistic cinematic film still"** sans cadre/marge/planche/légende. Bascule de modèle d'ancrage = changer uniquement `anchor` dans le manifest (l'ancien template mono-image `api/qwen_edit_i2i.json` reste sur disque, non référencé).
7. **Grille contact-sheet** (`submitGridJob` → `addGrid`) : job "fire-and-forget" (le pipeline ne l'attend pas, il continue vers les cuts) — cols adaptatif `ceil(sqrt(n))`.
8. **1 cut i2v par keyframe** (`submitCutJob`, template `api/ltx_i2v.json` tel quel) : le prompt du cut ancre l'enhancer LTX sur le contenu réel de la keyframe — `` `${scene}. ${charDesc}. ${locDesc}. Camera motion: ${motion}.` `` (scène du plan + identité personnage + description du décor + mouvement de caméra, voir LESSONS piège n°9) — la taille vidéo est dérivée de la taille réelle de la keyframe (`videoSizeFor`, côté long ≤1280, multiples de 32). Une **tenue du plan final** optionnelle (`holdDur > 0`) ajoute un cut supplémentaire sur la dernière keyframe avec le même ancrage scène+charDesc+locDesc et un mouvement figé ("Locked camera holding…") — implémentée simplement en poussant une case de plus dans `cutKeys`/`motions`, pas par un mécanisme dédié dans un builder JS.
9. **Assemblage** (`submitAnimaticJob` → `buildAnimaticGraph`) : **cuts francs** (pas d'interpolation FLF2V) — chaque cut vidéo est décomposé (`LoadVideo`→`GetVideoComponents`), les images concaténées (`ImageBatch`) et les pistes audio concaténées (`AudioConcat`) si l'audio est activé, puis réassemblées en un seul fichier (`CreateVideo`/`SaveVideo` → `studio/story/animatic`).

**Mode Réalisateur** (contrôle manifest `mode`, bouton `modeAutoBtn`/`modeDirectorBtn`, état `directorMode`/`director`) : mêmes fonctions d'étape que le mode Auto, mais avec une **ÉTAPE 0** de contrôle des deux planches d'ancrage avant tout le reste — `directorStep1` soumet immédiatement les jobs charsheet ET locsheet (pendant que gemma découpe les plans en parallèle) et affiche une carte par planche (`renderAnchorCard`, zone `anchorCardsWrap`) avec statut (Génération…/Prêt/Erreur) et un bouton 🔄 Régénérer (`regenerateAnchor` : reseed `+1` la planche concernée, resoumet, **ré-invalide** la session — `director.validated = false`) ; le bouton **"Valider les planches"** (`directorValidateBtn`) ne s'active que quand les deux planches sont `ready` (`anchorsReady`), et sa validation (`director.validated = true`) est la condition (`updateGateState`) qui révèle la liste de plans éditable et le bouton keyframes — toute régénération d'une planche après validation ré-invalide et re-masque la suite. Ensuite : `directorStep2` (1 carte par keyframe, **régénérable** — `regenerateKeyframe` reseed `+1` — ou **verrouillable** — `toggleLockKeyframe` — avant de continuer ; `directorLaunchBtn` ne s'active que quand toutes les cases sont `ready`), `directorStep3` (grille + cuts + assemblage, identique aux étapes 7-9 du mode Auto via les mêmes `submitGridJob`/`submitCutJob`/`submitAnimaticJob` — `directorStep3` déstructure `charDesc`/`locDesc` du même état `director` que l'étape 0 et les passe à `submitCutJob` exactement comme `generateStoryboardV2`, donc le même ancrage cut protège les deux modes).

**Qualification (rendu réel, 2026-07-17)** — run Auto N=8 (cuts 2 s + tenue 2 s, audio ON) : identité personnage cohérente sur les 8 keyframes (image fixe), grille 3×3 (3+3+2) équilibrée, animatic = exactement 9×49 = 441 frames (`ffprobe`, aucune dérive de comptage), audio aac continu 128 kbps sur toute la durée (pas de silence détecté aux coupes). Timings mesurés et projection N=16 : voir `workflows/README.md` et le rapport de qualification LOT 3.

**Risque CRITIQUE identifié (frames extraites en milieu de cut, pas seulement au premier/dernier point)** : les cuts vidéo (étape 6, LTX i2v) dérivent souvent vers un contenu **sans rapport** avec la keyframe/le personnage — sur le run N=8, seuls 3/9 cuts gardent clairement le personnage et le décor, 4/9 dérivent totalement (fontaine, rue, homme agenouillé, champ de blé). Cause probable : le prompt du cut est réduit à `shot.motion` seul (pas de scène/personnage), amplifié à l'aveugle par l'enhancer texte-only du template (`TextGenerateLTX2Prompt`, aucune entrée image) sous cfg 1 — voir LESSONS piège n°9 (détail, preuves, piste de correction). Ce risque touche la fidélité de fond de l'animatic, indépendamment de l'ancrage Qwen-Edit. Risque secondaire : l'ancrage Qwen-Edit (étape 4, image fixe) ne suit pas non plus les angles de caméra forts imposés (zénithal, contre-plongée extrême) — LESSONS piège n°8 ; sans impact en usage normal (gemma ne propose pas ce type d'angle de lui-même).

**Qualification LOT 4 (rendu réel, 2026-07-17, run dual complet N=4, brief astronaute)** — sortie de référence : `/home/sparks/comfyui-spark/basedir/output/studio/story/{charsheet_00009_,locsheet_00005_,key_01_00009_…key_04_00009_,grid_00021_,cut_01_00007_…cut_04_00006_,cut_05_00002_(tenue),animatic_00018_}`. Les 2 planches sont conformes au design attendu (turnaround 4 poses + portrait + rangée d'expressions + gros plans costume + bandeau couleurs pour `charsheet_00009_.png` ; plans larges multi-angles + gros plans matière pour `locsheet_00005_.png`), zéro texte parasite sur les deux. Les 4 keyframes conservent le même visage/coiffure/combinaison que le portrait de la charsheet ET le même site (ruines dans la jungle au coucher de soleil) que la locsheet — `key_03_00010_.png` fournit le plan rapproché du visage demandé. Frame médiane (`ffmpeg select=eq(n\,24)`) de chacun des 5 cuts (4 plans + tenue) confirme personnage ET décor tenus jusqu'au milieu du clip (aucune des captures ne montre de dérive), à comparer aux frames médianes pré-correctif (`/tmp/.../scratchpad/lot3/n8_cutframes/cut_01_00003__mid.png` = fontaine ornementale sans personnage, `cut_03_00003__mid.png` = homme en costume dans une rue — deux contenus totalement sans rapport). `ffprobe -count_frames` sur `animatic_00018_.mp4` : `nb_read_frames=245` = exactement 5×49 (5 cuts de 2 s), aucune dérive de comptage ; `ffmpeg silencedetect(-40dB,0.3s)` ne détecte qu'un silence en toute fin de piste (9.44 s→9.81 s, la queue des 0.4 dernières secondes sur 9.8 s totales) — aucune coupure entre cuts, piste aac 48 kHz stéréo continue. Verdict détaillé par axe (visage/décor/cuts) et comparaison avant/après avec chemins : voir LESSONS.md § Storyboard v2, sous piège n°10.

## Galerie

Onglets Images/Vidéos avec compteurs. `addAsset` (dédoublonnage `seenAssets`, masquage persistant `deletedAssets` en localStorage, filtre des fichiers audio seuls) → `addAssetCard` → `groupFor` regroupe `studio/story*` et `studio/campaign*` en encadrés par dossier. Lightbox intégré (images+vidéos) avec **copie du prompt** : `recordPrompts(entry)` remonte le graphe d'un job depuis chaque nœud de sortie (`promptForNode`, texte `CLIPTextEncode` le plus long = positif) et remplit `assetPrompts`. Actions cartes : agrandir/lire, utiliser en entrée (i2i/i2v — re-upload via `/view`→`/upload/image`), ➕ séquence (bascule scénario Storyboard + pipeline FLF2V), supprimer.

## i18n & thème

- i18n : dictionnaire `I18N` (clé = texte DOM d'origine) appliqué par `translateTree` (TreeWalker, `__orig` mémorisé par nœud texte) + `DYN_I18N` pour les chaînes à paramètres. Ajouter une chaîne = l'écrire en français dans le DOM + une entrée I18N.
- Thème : variables CSS dans `:root`, overrides `:root[data-theme="dark"]`. **Ne jamais coder une couleur en dur dans un composant** — passer par les variables (`--card`, `--bg-light`, …).

## Stockage navigateur

| Clé | Store | Contenu |
|---|---|---|
| `theme`, `lang`, `cloudMode`, `cloudPlatform` | localStorage | préférences |
| `deletedAssets` | localStorage | clés d'assets masqués |
| `cloudKey:<plateforme>` | **sessionStorage** | clés API (jamais persistées) |

## Ajouter un modèle / workflow (outillé)

`tools/onboard.py <workflow_ui.json> --id <id> --label "<label>" --pipeline <p> --model "<nom>" [--fps N] [--enrich builtin]` fait toute la chaîne : rafraîchit `object_info`, convertit le format UI en API (`convert.py`), **injecte les placeholders** (`{{PROMPT}}`/`{{NEGATIVE_PROMPT}}` sur les CLIPTextEncode positif/négatif — ou l'input `prompt` du `TextGenerateLTX2Prompt` s'il est sur le chemin —, `{{SEED}}`, `{{WIDTH}}/{{HEIGHT}}/{{BATCH}}` des `Empty*Latent*`, `{{FRAMES}}` des latents vidéo, `{{IMAGE}}` des `LoadImage`), vérifie les `.safetensors` sur disque, écrit `workflows/api/<id>.json`, ajoute l'entrée manifest, puis lance `validate.py --reduce`. Le workflow source vient soit de `workflows/*.json`, soit d'un template officiel du conteneur (`docker exec comfyui-nvidia cat …/templates/<x>.json`).

`tools/validate.py <api.json> [--reduce] [--frames 0,12,24] [--audio] [--image <fichier>]` : vérif structurelle (nœuds dans `object_info`, liens intègres, modèles sur disque) → substitution des placeholders par des valeurs de test → soumission → poll → extraction frames/mp3 pour inspection. **Toujours regarder le contenu produit**, pas seulement le statut (cf. `docs/TESTING.md`).

**Nouveau pipeline** (jeu de contrôles UI inédit) : ajouter une entrée à `pipelines[]` du manifest (`controls`, `scenario?`, `fps?`) — aucun JS à toucher si les contrôles existent déjà. **Nouveau builder JS** : une fonction `(seed, width, height)` + une entrée dans le registre `BUILDERS` + `{file:null, builder:"<nom>"}` au manifest.

### Monter un modèle en version supérieure
Ré-onboarder le template officiel de la nouvelle version sous un id suffixé (`_v2`) au lieu d'écraser : `onboard.py … --id <base>_v2`. Les deux apparaissent alors dans le menu Modèle → rendu **A/B côte à côte** dans la galerie (même brief/seed). Une fois la nouvelle version validée, retirer/renommer l'ancienne entrée manifest (le fichier api reste, réactivable en 1 ligne). Piège récurrent : un modèle plus gros exige souvent un autre encodeur (cf. Flux2 Klein 9B ⇒ `qwen_3_8b_fp8mixed`, LESSONS) — `onboard.py` détecte les fichiers manquants, `validate.py` attrape au rendu les incompatibilités de dimensions que la vérif structurelle laisse passer.
